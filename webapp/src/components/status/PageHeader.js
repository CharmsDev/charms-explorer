'use client';

export default function PageHeader() {
    return (
        <div className="mb-6">
            <h1 className="text-3xl font-bold bg-gradient-to-r from-primary-400 to-primary-600 bg-clip-text text-transparent">
                Blockchain Status
            </h1>
            <div className="h-1 w-20 bg-gradient-to-r from-primary-400 to-primary-600 rounded-full mt-2"></div>
        </div>
    );
}
