# Charms Explorer - Webapp Environment Variables
#
# Production values are in wrangler.toml (Cloudflare Pages).
# This file is for local development only.

# Explorer API (v1 — versioned)
NEXT_PUBLIC_CHARMS_API_URL=http://localhost:8000/v1

# QuickNode Bitcoin RPC (Mainnet) — failover for tx lookups
# If not configured, falls back to Mempool.space API
NEXT_PUBLIC_QUICKNODE_BITCOIN_MAINNET_URL=https://your-endpoint.btc.quiknode.pro/your-token/
NEXT_PUBLIC_QUICKNODE_BITCOIN_MAINNET_API_KEY=your-api-key-here
